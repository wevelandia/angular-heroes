export const environments = {
  // Apuntando a la base en Desarrollo
  baseURL: 'http://localhost:3000'
}

// Para que Angular sepa de esos archivos se configuran en angular json en la parte de:
// configurations - production - fileReplacements
/* "fileReplacements": [
  {
    "replace": "src/environments/environments.ts",
    "with": "src/environments/environments.prod.ts"
  }
], */
